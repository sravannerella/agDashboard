export class Card {

    constructor(
        private title: String,
        private label: String,
        private labelData: String,
        private tag: String,
        private subtext: String) { }
}
